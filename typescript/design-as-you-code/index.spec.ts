import { describe, expect, it } from 'vitest';

describe('design as you code', () => {
    it('sum two numbers', () => {
        const a = 1;
        const b = 2;
        const sum = a + b;
        expect(sum).toBe(3);
    })
});